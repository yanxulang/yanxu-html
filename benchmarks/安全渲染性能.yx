引「包:yanxu-html」为 HTML；

引「标准:JSON」为 JSON；

引「标准:数学」为 数学；

引「标准:时间」为 时间；

引「标准:环境」为 环境；

引「标准:文字」为 文字；

定 参数列：列<文> 为 环境.参数（）；

若 （长度（参数列） 大于 1） 则
    抛 「HTML_BENCHMARK_ARGUMENT：只接受可选轮数」；
终

令 轮数：数 为 50；

若 （长度（参数列） 等于 1） 则
    置 轮数 为 JSON.解析（参数列【0】）；
终

若 （（（数学.下取整（轮数） 不等于 轮数） 或 （轮数 小于 1）） 或 （轮数 大于 500）） 则
    抛 「HTML_BENCHMARK_ROUNDS：轮数须为 1 至 500 的整数」；
终

令 各文章：列 为 【】；

令 文章号：数 为 0；

当 （文章号 小于 20） 则
    追加（各文章，HTML.元素（「article」，【HTML.属性（「class」，「entry」），HTML.属性（「data-id」，JSON.序列化（文章号））】，【HTML.元素（「h2」，【】，【HTML.文字（（（「文章 <」 加 JSON.序列化（文章号）） 加 「>」））】），HTML.元素（「p」，【】，【HTML.文字（「包含 & 符号、引号 " 与中文的稳定渲染负载」）】），HTML.元素（「a」，【HTML.属性（「href」，（「/posts/」 加 JSON.序列化（文章号）））】，【HTML.文字（「阅读全文」）】）】））；
    置 文章号 为 （文章号 加 1）；
终

定 页面 为 HTML.文档（HTML.元素（「html」，【HTML.属性（「lang」，「zh-CN」）】，【HTML.元素（「head」，【】，【HTML.元素（「meta」，【HTML.属性（「charset」，「utf-8」）】，【】），HTML.元素（「title」，【】，【HTML.文字（「渲染基准」）】）】），HTML.元素（「body」，【】，【HTML.元素（「main」，【】，【HTML.片段（各文章）】）】）】））；

定 完整开始：数 为 时间.毫秒（）；

令 完整字符：数 为 0；

令 次：数 为 0；

当 （次 小于 轮数） 则
    置 完整字符 为 （完整字符 加 长度（文字.字符列（页面.渲染（））））；
    置 次 为 （次 加 1）；
终

定 完整毫秒：数 为 （时间.毫秒（） 减 完整开始）；

令 写入字符：数 为 0；

法 计片（片段：文）：空 则
    置 写入字符 为 （写入字符 加 长度（文字.字符列（片段）））；
    归 空；
终

定 增量开始：数 为 时间.毫秒（）；

令 增量字符：数 为 0；

令 增量片段：数 为 0；

置 次 为 0；

当 （次 小于 轮数） 则
    定 统计：典 为 HTML.流式渲染（页面，计片，{}）；
    置 增量字符 为 （增量字符 加 统计【「字符数」】）；
    置 增量片段 为 （增量片段 加 统计【「片段数」】）；
    置 次 为 （次 加 1）；
终

定 增量毫秒：数 为 （时间.毫秒（） 减 增量开始）；

若 （（完整字符 不等于 增量字符） 或 （增量字符 不等于 写入字符）） 则
    抛 「HTML_BENCHMARK_CHECKSUM：完整与增量渲染字符校验和不一致」；
终

若 （（完整字符 不大于 0） 或 （增量片段 不大于 0）） 则
    抛 「HTML_BENCHMARK_EMPTY：渲染负载不得为空」；
终

言 {「轮数」：轮数，「文章数」：长度（各文章），「完整毫秒」：完整毫秒，「完整字符」：完整字符，「增量毫秒」：增量毫秒，「增量字符」：增量字符，「增量片段」：增量片段}；
