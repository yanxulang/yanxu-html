引「../src/言序HTML.yx」为 HTML；

引「标准:测试」为 测试；

法 错误片段（） 则
    HTML.片段（【「未包装文字」】）；
    归 空；
终

法 丢片（片：文）：空 则
    归 空；
终

法 取代码（动作：法）：文 则
    试 则
        动作（）；
    救 所误 则
        归 HTML.错误详情（所误）【「代码」】；
    终
    归 「」；
终

令 外部子项：列 为 【HTML.文字（「甲」），HTML.元素（「br」，【】，【】）】；

定 所片段 为 HTML.片段（外部子项）；

追加（外部子项，HTML.文字（「外部」））；

测试.相等（所片段.渲染（），「甲<br>」）；

所片段.添子（HTML.文字（「乙」））；

测试.相等（所片段.渲染（），「甲<br>乙」）；

测试.相等（长度（所片段.各子节点（）），3）；

定 子项快照 为 所片段.各子节点（）；

追加（子项快照，HTML.文字（「快照」））；

测试.相等（长度（所片段.各子节点（）），3）；

测试.相等（取代码（错误片段），「HTML_NODE」）；

定 页面 为 HTML.文档（HTML.元素（「html」，【HTML.属性（「lang」，「zh-CN」）】，【HTML.元素（「head」，【】，【HTML.元素（「meta」，【HTML.属性（「charset」，「utf-8」）】，【】），HTML.元素（「title」，【】，【HTML.文字（「言序 <HTML>」）】）】），HTML.元素（「body」，【HTML.属性（「class」，「page」）】，【HTML.片段（【HTML.元素（「h1」，【】，【HTML.文字（「安全页面」）】），HTML.元素（「a」，【HTML.属性（「href」，「/docs?q=<安全>」）】，【HTML.文字（「查看 & 学习」）】）】）】）】））；

定 页面快照：文 为 「<!doctype html>\n<html lang="zh-CN"><head><meta charset="utf-8"><title>言序 &lt;HTML&gt;</title></head><body class="page"><h1>安全页面</h1><a href="/docs?q=&lt;安全&gt;">查看 &amp; 学习</a></body></html>」；

测试.相等（页面.渲染（），页面快照）；

测试.相等（HTML.安全渲染（页面），页面快照）；

定 片段统计：典 为 HTML.流式渲染（HTML.片段（【HTML.文字（「甲」），HTML.文字（「乙」）】），丢片，{}）；

测试.相等（片段统计【「节点数」】，3）；
