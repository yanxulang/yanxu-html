引「../src/言序HTML.yx」为 HTML；

引「标准:测试」为 测试；

法 构建卡片（） 则
    归 HTML.元素（「article」，【HTML.属性（「class」，「card」）】，【HTML.文字（「内容」）】）；
终

法 构建错误值（） 则
    归 「不是节点」；
终

法 错误属性列（） 则
    HTML.元素（「div」，【「不是属性」】，【】）；
    归 空；
终

法 错误子节点列（） 则
    HTML.元素（「div」，【】，【「未包装文字」】）；
    归 空；
终

法 错误组件（） 则
    HTML.组件（构建错误值）.渲染（）；
    归 空；
终

法 须失败（动作：法，说明：文）：空 则
    令 已失败：理 为 假；
    试 则
        动作（）；
    救 所误 则
        置 已失败 为 真；
    终
    测试.断言（已失败，说明）；
    归 空；
终

定 卡片 为 HTML.组件（构建卡片）；

测试.相等（卡片.构建（）.渲染（），「<article class="card">内容</article>」）；

测试.相等（卡片.渲染（），「<article class="card">内容</article>」）；

定 容器 为 HTML.元素（「section」，【】，【】）；

容器.添属性（HTML.属性（「data-kind」，「demo」））.添子（HTML.文字（「甲」））.添子（HTML.原始（「<b>乙</b>」））；

测试.相等（容器.渲染（），「<section data-kind="demo">甲<b>乙</b></section>」）；

测试.相等（HTML.安全渲染（容器），容器.渲染（））；

测试.相等（HTML.属性（「checked」，真）.渲染（），「 checked」）；

测试.相等（HTML.属性（「checked」，假）.渲染（），「」）；

测试.相等（HTML.属性（「title」，空）.渲染（），「」）；

测试.相等（HTML.文档（容器）.渲染（），「<!doctype html>\n<section data-kind="demo">甲<b>乙</b></section>」）；

须失败（错误属性列，「元素必须拒绝非属性值」）；

须失败（错误子节点列，「元素必须拒绝未包装文字」）；

须失败（错误组件，「组件必须拒绝非节点返回值」）；
