引「../src/言序HTML.yx」为 HTML；

引「标准:测试」为 测试；

法 脚本标签（） 则
    HTML.元素（「script」，【】，【HTML.文字（「alert(1)」）】）；
    归 空；
终

法 样式标签（） 则
    HTML.元素（「STYLE」，【】，【HTML.文字（「body{}」）】）；
    归 空；
终

法 嵌入标签（） 则
    HTML.元素（「iframe」，【】，【】）；
    归 空；
终

法 矢量标签（） 则
    HTML.元素（「svg」，【】，【】）；
    归 空；
终

法 事件属性（） 则
    HTML.属性（「onload」，「alert(1)」）；
    归 空；
终

法 混合事件属性（） 则
    HTML.属性（「OnClick」，「alert(1)」）；
    归 空；
终

法 样式属性（） 则
    HTML.属性（「style」，「background:url(x)」）；
    归 空；
终

法 文档属性（） 则
    HTML.属性（「srcdoc」，「<script>」）；
    归 空；
终

法 刷新属性（） 则
    HTML.属性（「http-equiv」，「refresh」）；
    归 空；
终

法 响应图属性（） 则
    HTML.属性（「srcset」，「a.png 1x, b.png 2x」）；
    归 空；
终

法 重复属性（） 则
    HTML.元素（「div」，【HTML.属性（「class」，「a」），HTML.属性（「CLASS」，「b」）】，【】）；
    归 空；
终

法 重复追加（） 则
    HTML.元素（「div」，【HTML.属性（「id」，「a」）】，【】）.添属性（HTML.属性（「ID」，「b」））；
    归 空；
终

法 须失败（动作：法，说明：文）：空 则
    令 已失败：理 为 假；
    试 则
        动作（）；
    救 所误 则
        置 已失败 为 真；
    终
    测试.断言（已失败，说明）；
    归 空；
终

须失败（脚本标签，「安全元素必须拒绝 script」）；

须失败（样式标签，「安全元素必须大小写不敏感地拒绝 style」）；

须失败（嵌入标签，「安全元素必须拒绝 iframe」）；

须失败（矢量标签，「没有子语言策略时必须拒绝 SVG」）；

须失败（事件属性，「安全属性必须拒绝事件处理器」）；

须失败（混合事件属性，「事件属性拒绝必须大小写不敏感」）；

须失败（样式属性，「没有 CSS 解析器时必须拒绝 style」）；

须失败（文档属性，「安全属性必须拒绝 srcdoc」）；

须失败（刷新属性，「安全属性必须拒绝 http-equiv」）；

须失败（响应图属性，「没有 srcset 解析器时必须拒绝 srcset」）；

须失败（重复属性，「构造时必须拒绝大小写不敏感的重复属性」）；

须失败（重复追加，「追加时必须拒绝大小写不敏感的重复属性」）；

测试.相等（HTML.原始（「<script>trusted()</script>」）.渲染（），「<script>trusted()</script>」）；

测试.相等（HTML.元素（「div」，【HTML.属性（「data-id」，「42」），HTML.属性（「aria-label」，「说明」）】，【】）.渲染（），「<div data-id="42" aria-label="说明"></div>」）；

令 外部属性：列 为 【HTML.属性（「class」，「first」）】；

令 外部子项：列 为 【HTML.文字（「甲」）】；

定 已隔离 为 HTML.元素（「div」，外部属性，外部子项）；

追加（外部属性，HTML.属性（「title」，「外部」））；

追加（外部子项，HTML.文字（「乙」））；

测试.相等（已隔离.渲染（），「<div class="first">甲</div>」）；

定 属性快照 为 已隔离.各属性（）；

定 子项快照 为 已隔离.各子节点（）；

追加（属性快照，HTML.属性（「title」，「快照」））；

追加（子项快照，HTML.文字（「丙」））；

测试.相等（长度（已隔离.各属性（）），1）；

测试.相等（长度（已隔离.各子节点（）），1）；

测试.相等（已隔离.渲染（），「<div class="first">甲</div>」）；
