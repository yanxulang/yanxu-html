引「../src/言序HTML.yx」为 HTML；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

令 流片：列<文> 为 【】；

法 收片（片段：文）：空 则
    追加（流片，片段）；
    归 空；
终

法 写扩展（写入法：法）：空 则
    写入法（「<mark>」）；
    写入法（「可信扩展」）；
    写入法（「</mark>」）；
    归 空；
终

法 未知限制（） 则
    HTML.规范限制（{「未知」：1}）；
    归 空；
终

法 小数限制（） 则
    HTML.规范限制（{「最大节点」：1.5}）；
    归 空；
终

法 零节点限制（） 则
    HTML.规范限制（{「最大节点」：0}）；
    归 空；
终

法 过大限制（） 则
    HTML.规范限制（{「最大输出字符」：16777217}）；
    归 空；
终

法 输出超限（） 则
    HTML.渲染受限（HTML.文字（「<」），{「最大输出字符」：3}）；
    归 空；
终

法 深度超限（） 则
    定 根 为 HTML.元素（「div」，【】，【HTML.元素（「span」，【】，【HTML.文字（「深」）】）】）；
    HTML.渲染受限（根，{「最大深度」：1}）；
    归 空；
终

法 节点超限（） 则
    定 根 为 HTML.元素（「div」，【】，【HTML.文字（「甲」），HTML.文字（「乙」）】）；
    HTML.渲染受限（根，{「最大节点」：2}）；
    归 空；
终

法 属性超限（） 则
    定 根 为 HTML.元素（「div」，【HTML.属性（「id」，「a」），HTML.属性（「class」，「b」）】，【】）；
    HTML.渲染受限（根，{「最大属性」：1}）；
    归 空；
终

法 片段超限（） 则
    HTML.渲染受限（HTML.元素（「div」，【】，【HTML.文字（「甲」）】），{「最大片段」：2}）；
    归 空；
终

法 取代码（动作：法）：文 则
    试 则
        动作（）；
    救 所误 则
        归 HTML.错误详情（所误）【「代码」】；
    终
    归 「」；
终

定 默认：典 为 HTML.默认限制（）；

测试.相等（默认【「最大深度」】，HTML.默认最大渲染深度）；

测试.相等（默认【「最大节点」】，HTML.默认最大渲染节点）；

测试.相等（默认【「最大属性」】，HTML.默认最大元素属性）；

测试.相等（默认【「最大输出字符」】，HTML.默认最大输出字符）；

测试.相等（默认【「最大片段」】，HTML.默认最大输出片段）；

测试.相等（取代码（未知限制），「HTML_LIMIT_CONFIG」）；

测试.相等（取代码（小数限制），「HTML_LIMIT_CONFIG」）；

测试.相等（取代码（零节点限制），「HTML_LIMIT_CONFIG」）；

测试.相等（取代码（过大限制），「HTML_LIMIT_CONFIG」）；

测试.相等（取代码（输出超限），「HTML_LIMIT_OUTPUT」）；

测试.相等（取代码（深度超限），「HTML_LIMIT_DEPTH」）；

测试.相等（取代码（节点超限），「HTML_LIMIT_NODE」）；

测试.相等（取代码（属性超限），「HTML_LIMIT_ATTRIBUTE」）；

测试.相等（取代码（片段超限），「HTML_LIMIT_FRAGMENT」）；

测试.相等（HTML.渲染受限（HTML.文字（「<」），{「最大输出字符」：4}），「&lt;」）；

定 流节点 为 HTML.元素（「div」，【HTML.属性（「class」，「box」）】，【HTML.文字（「甲」），HTML.文字（「乙」）】）；

置 流片 为 【】；

定 统计：典 为 HTML.流式渲染（流节点，收片，{}）；

测试.相等（文字.联结（流片，「」），「<div class="box">甲乙</div>」）；

测试.相等（统计【「节点数」】，3）；

测试.相等（统计【「字符数」】，长度（文字.字符列（「<div class="box">甲乙</div>」）））；

测试.相等（统计【「片段数」】，6）；

定 扩展节点 为 HTML.扩展（写扩展）；

测试.相等（扩展节点.渲染（），「<mark>可信扩展</mark>」）；

置 流片 为 【】；

定 扩展统计：典 为 HTML.流式渲染（扩展节点，收片，{}）；

测试.相等（文字.联结（流片，「」），「<mark>可信扩展</mark>」）；

测试.相等（扩展统计【「节点数」】，1）；

测试.相等（扩展统计【「片段数」】，3）；
