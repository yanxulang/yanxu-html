引「../src/言序HTML.yx」为 HTML；

引「标准:测试」为 测试；

法 非法标签（） 则
    HTML.元素（「1div」，【】，【】）；
    归 空；
终

法 危险标签（） 则
    HTML.元素（「script」，【】，【】）；
    归 空；
终

法 危险属性（） 则
    HTML.属性（「onclick」，「x」）；
    归 空；
终

法 危险地址（） 则
    HTML.属性（「href」，「javascript:x」）；
    归 空；
终

法 重复属性（） 则
    HTML.元素（「div」，【HTML.属性（「id」，「a」），HTML.属性（「ID」，「b」）】，【】）；
    归 空；
终

法 空元素（） 则
    HTML.元素（「br」，【】，【HTML.文字（「x」）】）；
    归 空；
终

法 错误组件值（） 则
    归 「x」；
终

法 错误组件（） 则
    HTML.组件（错误组件值）.渲染（）；
    归 空；
终

法 抽象节点（） 则
    HTML.HTML节点（）.渲染（）；
    归 空；
终

法 外部错误（） 则
    抛 「外部失败」；
终

法 取错误（动作：法）：典 则
    试 则
        动作（）；
    救 所误 则
        归 HTML.错误详情（所误）；
    终
    归 {}；
终

测试.相等（取错误（非法标签）【「代码」】，「HTML_TAG」）；

测试.相等（取错误（危险标签）【「代码」】，「HTML_TAG_UNSAFE」）；

测试.相等（取错误（危险属性）【「代码」】，「HTML_ATTRIBUTE_UNSAFE」）；

测试.相等（取错误（危险地址）【「代码」】，「HTML_URL」）；

测试.相等（取错误（重复属性）【「代码」】，「HTML_ATTRIBUTE_DUPLICATE」）；

测试.相等（取错误（空元素）【「代码」】，「HTML_VOID」）；

测试.相等（取错误（错误组件）【「代码」】，「HTML_COMPONENT」）；

测试.相等（取错误（抽象节点）【「代码」】，「HTML_NODE_ABSTRACT」）；

定 外部详情：典 为 取错误（外部错误）；

测试.相等（外部详情【「代码」】，「HTML_RUNTIME」）；

测试.相等（外部详情【「消息」】，「外部失败」）；

定 成功：典 为 HTML.尝试渲染（HTML.文字（「<安全>」））；

测试.相等（成功【「成功」】，真）；

测试.相等（成功【「值」】，「&lt;安全&gt;」）；

测试.相等（成功【「错误」】，空）；

定 失败：典 为 HTML.尝试渲染（HTML.HTML节点（））；

测试.相等（失败【「成功」】，假）；

测试.相等（失败【「值」】，空）；

测试.相等（失败【「错误」】【「代码」】，「HTML_NODE_ABSTRACT」）；
